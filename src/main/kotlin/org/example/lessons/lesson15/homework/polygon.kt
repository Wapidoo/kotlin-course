package org.example.org.example.lessons.lesson15.homework

open class polygon(
    area: Double,
    symmetry: Int,
    flat: Boolean,
    val simple: Boolean,
    val angleCount: Int
): geometryFigure(area, symmetry, flat)